import { Col, Row } from './components'

const drag = {
  BUTTON: {
    COLUMN: 'button col',
    ROW: 'button row',
  },
  COLUMN: 'col',
  ROW: 'row',
  IMAGE: 'image',
  TIMER: 'timer',
  VARIABLE: 'variable',
}

const tag = {
  Col,
  Row,
}

export const types = {
  drag,
  tag,
}
