{
  "name": "single-studio",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for Single Studio: a local-first broadcast graphics framework for OBS.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r --filter \"./packages/*\" build",
    "demo": "pnpm --filter @single-studio/demo dev",
    "demo:build": "pnpm --filter @single-studio/demo build",
    "lint": "eslint .",
    "format": "prettier --write \"**/*.{js,jsx,json,css,md,yaml,yml}\"",
    "test": "pnpm -r test",
    "test:watch": "pnpm --filter @single-studio/core test:watch"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^9.15.0",
    "eslint-plugin-react": "^7.37.2",
    "eslint-plugin-react-hooks": "^5.0.0",
    "globals": "^15.12.0",
    "playwright": "^1.62.1",
    "prettier": "^3.4.1"
  }
}
