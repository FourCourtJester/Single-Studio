/**
 * The name this studio's data is stored under.
 *
 * Deliberately not `demo`: this show keeps its own database, its own worker and its
 * own image store, so running it beside the fixture cannot cross the two.
 */
export const STUDIO_ID = 'vainglory'
